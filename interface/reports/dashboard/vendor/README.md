Vendor JS files here if your environment blocks CDN.

Quick start (from repo root):
- python interface/reports/dashboard/fetch_vendor.py

Then edit interface/reports/dashboard/index.html to use local scripts instead of CDN.
